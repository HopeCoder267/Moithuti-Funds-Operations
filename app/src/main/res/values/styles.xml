<resources>
    <!-- Theme for MainActivity without action bar -->
    <style name="Theme.MoithutiFundsOperations.NoActionBar" parent="Theme.MoithutiFundsOperations">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Card styles -->
    <style name="Widget.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="cardBackgroundColor">@color/card_background</item>
    </style>

    <!-- Status indicator styles -->
    <style name="StatusIndicator">
        <item name="android:layout_width">12dp</item>
        <item name="android:layout_height">12dp</item>
        <item name="android:background">@drawable/status_circle</item>
    </style>

    <!-- Stat card text styles -->
    <style name="StatCardTitle">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:layout_marginBottom">4dp</item>
    </style>

    <style name="StatCardValue">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
